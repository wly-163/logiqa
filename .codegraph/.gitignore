# CodeGraph data files — local to each machine, not for committing.
# Ignore everything in .代码审计/ except this file itself, so transient
# files (the database, daemon.pid, sockets, logs) never show up in git.
*
!.gitignore
